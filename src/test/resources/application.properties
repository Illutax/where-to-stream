spring.datasource.url=jdbc:h2:mem:testdb
wer-streamt.path=src/test/resources/test-assets
# Schema is created by Liquibase; Hibernate validates against it (same as production).
spring.jpa.hibernate.ddl-auto=validate
