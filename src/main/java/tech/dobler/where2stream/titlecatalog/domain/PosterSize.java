package tech.dobler.where2stream.titlecatalog.domain;

/** The two poster sizes the app serves: a small thumbnail for the row, a larger one for the hover. */
public enum PosterSize {
    THUMB,
    FULL
}
