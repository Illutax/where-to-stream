package tech.dobler.werstreamt.domain;

/**
 * A user's preferred library layout. {@link #GRID} is the poster-tile grid;
 * {@link #LIST} is the sortable table.
 */
public enum ViewMode {
    LIST,
    GRID
}
